# movies-galore-server
Apollo server for https://github.com/nzuqi/movies-galore project.

## Run on local
Use `$ node index` run on localhost. By deafault, it runs on port `:4000`

## Deploy to heroku
Use `$ git push heroku master` to deploy to heroku.